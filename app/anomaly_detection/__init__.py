"""Anomaly-detection helpers used by the api at export time (Step 14.8).

This package intentionally lives in `app/` (not `backend/allotrope/`) so the
science code is portable — same pattern as `app/spectral_match/`.
"""
