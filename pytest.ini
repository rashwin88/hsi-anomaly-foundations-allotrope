[pytest]
markers =
    large_files: Tests that require large local files
    large_benchmarks: Tests that require very heavy benchmarking
    network_access: Tests that require network access to function
